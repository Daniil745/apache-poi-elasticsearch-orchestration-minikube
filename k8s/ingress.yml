# Kubernetes Ingress configuration for external HTTP access to DocSearch API.
# Routes all traffic to doc-search-app service on port 8080.
# Sets 50MB proxy body size limit for document file uploads via nginx annotation.

apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: doc-search-ingress
  namespace: doc-search
  annotations:
    nginx.ingress.kubernetes.io/proxy-body-size: "50m"
spec:
  ingressClassName: nginx
  rules:
    - http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: doc-search-app
                port:
                  number: 8080
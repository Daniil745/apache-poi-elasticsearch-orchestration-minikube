version: '3.8'

x-common-logging: &common-logging
  driver: "json-file"
  options:
    max-size: "10m"
    max-file: "3"

x-common-healthcheck: &common-healthcheck
  interval: 30s
  timeout: 10s
  retries: 5
  start_period: 40s

services:
  elasticsearch:
    image: docker.elastic.co/elasticsearch/elasticsearch:${ELASTIC_VERSION:-8.11.0}
    container_name: docsearch-elasticsearch
    restart: unless-stopped
    logging: *common-logging
    environment:
      - cluster.name=${ES_CLUSTER_NAME:-doc-search-cluster}
      - node.name=${ES_NODE_NAME:-node-1}
      - discovery.type=single-node
      - xpack.security.enabled=false
      - xpack.security.enrollment.enabled=false
      - xpack.security.http.ssl.enabled=false
      - xpack.security.transport.ssl.enabled=false
      - ES_JAVA_OPTS=-Xms${ES_HEAP_SIZE:-128m} -Xmx${ES_HEAP_SIZE:-256m}
      - indices.memory.index_buffer_size=20%
      - indices.queries.cache.size=10%
      - network.host=0.0.0.0
      - http.port=${ES_PORT_HTTP:-9200}
      - transport.port=${ES_PORT_TRANSPORT:-9300}
      - path.data=/usr/share/elasticsearch/data
      - path.logs=/usr/share/elasticsearch/logs
      - bootstrap.memory_lock=true
      - http.cors.enabled=true
      - http.cors.allow-origin="*"
    expose:
      - "9200"
      - "9300"
    volumes:
      - elasticsearch-data:/usr/share/elasticsearch/data
      - elasticsearch-logs:/usr/share/elasticsearch/logs
    ulimits:
      memlock:
        soft: -1
        hard: -1
      nofile:
        soft: 65536
        hard: 65536
    healthcheck:
      test: ["CMD-SHELL", "curl -s http://localhost:9200/_cluster/health?local=true | grep -q 'green\\|yellow'"]
      <<: *common-healthcheck
    networks:
      - elastic
    labels:
      - "service=elasticsearch"

  ollama:
    image: ollama/ollama:${OLLAMA_VERSION:-0.6.5}
    container_name: docsearch-ollama
    restart: unless-stopped
    logging: *common-logging
    ports:
      - "11434:11434"
    volumes:
      - ollama-data:/root/.ollama
      - ollama-models:/root/.ollama/models
    environment:
      - OLLAMA_HOST=0.0.0.0
      - OLLAMA_KEEP_ALIVE=1m
      - OLLAMA_NUM_PARALLEL=1
      - OLLAMA_MODEL=qwen2.5-coder:1.5b
    entrypoint: [ "sh", "-c" ]
    command:
      - |
        ollama serve &
        sleep 5
        echo "Pulling qwen2.5-coder:1.5b model..."
        ollama pull qwen2.5-coder:1.5b
        echo "Model ready!"
        wait
    healthcheck:
      test: [ "CMD", "curl", "-f", "http://localhost:11434/api/tags" ]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 120s
    networks:
      - elastic
    labels:
      - "service=ollama"

  app:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        APP_PROFILE: ${APP_PROFILE:-dev}
    image: docsearch-app:latest
    container_name: docsearch-app
    restart: unless-stopped
    logging: *common-logging
    environment:
      - SPRING_PROFILES_ACTIVE=${APP_PROFILE:-dev}
      - SERVER_PORT=8080
      - SPRING_ELASTICSEARCH_URIS=http://elasticsearch:9200
      - SPRING_DATASOURCE_URL=jdbc:h2:file:/data/docsearchdb;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
      - SPRING_DATASOURCE_USERNAME=sa
      - SPRING_DATASOURCE_PASSWORD=
      - SPRING_H2_CONSOLE_ENABLED=true
      - SPRING_JPA_HIBERNATE_DDL_AUTO=update
      - SPRING_JPA_SHOW_SQL=false
      - SPRING_JPA_OPEN_IN_VIEW=false
      - SPRING_SERVLET_MULTIPART_MAX_FILE_SIZE=50MB
      - SPRING_SERVLET_MULTIPART_MAX_REQUEST_SIZE=50MB
      - STORAGE_UPLOAD_DIR=/data/uploads
      - JAVA_OPTS=-Xms128m -Xmx256m -XX:+UseG1GC -XX:MaxGCPauseMillis=200
      - MANAGEMENT_ENDPOINTS_WEB_EXPOSURE_INCLUDE=health,info,metrics,prometheus
      - MANAGEMENT_ENDPOINT_HEALTH_SHOW_DETAILS=always
      - SPRING_AI_OLLAMA_BASE_URL=http://ollama:11434
      - SPRING_AI_OLLAMA_CHAT_MODEL=qwen2.5-coder:1.5b
      - SPRING_AI_OLLAMA_CHAT_OPTIONS_MODEL=qwen2.5-coder:1.5b
      - SPRING_AI_OLLAMA_EMBEDDINGS_MODEL=qwen2.5-coder:1.5b
    expose:
      - "8080"
    volumes:
      - app-data:/data
      - app-logs:/app/logs
    depends_on:
      elasticsearch:
        condition: service_healthy
    healthcheck:
      test: ["CMD-SHELL", "curl -s http://localhost:8080/actuator/health | grep -q 'UP'"]
      <<: *common-healthcheck
    networks:
      - elastic
    labels:
      - "service=app"

  h2-console:
    image: oscarfonts/h2:latest
    container_name: docsearch-h2
    restart: unless-stopped
    profiles:
      - dev
      - debug
    environment:
      - H2_OPTIONS=-ifNotExists -web -webAllowOthers -webPort 9092
    expose:
      - "9092"
    networks:
      - elastic
    labels:
      - "service=h2-console"

  nginx:
    image: nginx:1.25-alpine
    container_name: docsearch-nginx
    restart: unless-stopped
    logging: *common-logging
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf:ro
      - ./nginx/conf.d:/etc/nginx/conf.d:ro
      - ./nginx/ssl:/etc/nginx/ssl:ro
      - nginx-logs:/var/log/nginx
    depends_on:
      - app
    healthcheck:
      test: ["CMD-SHELL", "curl -f http://localhost/health || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 3
    networks:
      - elastic
    labels:
      - "service=nginx"

volumes:
  elasticsearch-data:
    driver: local
    name: docsearch_es_data
  elasticsearch-logs:
    driver: local
    name: docsearch_es_logs
  app-data:
    driver: local
    name: docsearch_app_data
  app-logs:
    driver: local
    name: docsearch_app_logs
  nginx-logs:
    driver: local
    name: docsearch_nginx_logs
  ollama-data:
    driver: local
    name: docsearch_ollama_data
  ollama-models:
    driver: local
    name: docsearch_ollama_models

networks:
  elastic:
    driver: bridge
    name: docsearch_network
    ipam:
      config:
        - subnet: 172.28.0.0/16